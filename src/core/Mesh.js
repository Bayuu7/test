export class Mesh {
  constructor(){
    this.vertices = [];
    this.normals = [];
    this.indices = [];
    this.position = [0,0,0];
  }

  draw(gl, camera, lights){
    // placeholder: nanti pakai WebGL shader dan buffer
  }
}
